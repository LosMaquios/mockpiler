{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "tsdx": "^0.13.3"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "tsdx test",
    "test:ci": "tsdx test --runInBand --ci"
  }
}
